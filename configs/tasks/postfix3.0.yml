---
# Version 3.0
# Postfix hardening and fixes derived from troubleshooting session

- name: Configure Postfix core settings
  command:
    argv:
      - postconf
      - -e
      - "{{ item }}"
  loop:
    - "smtpd_delay_reject = yes"
    - "unknown_local_recipient_reject_code = 550"
    - "smtpd_reject_unlisted_recipient = yes"
    - "mynetworks = 127.0.0.0/8 [::1]/128"
    - "smtpd_relay_restrictions = permit_mynetworks, permit_sasl_authenticated, reject_unauth_destination"
    - "smtpd_recipient_restrictions = permit_mynetworks, permit_sasl_authenticated, reject_non_fqdn_recipient, reject_unknown_recipient_domain, reject_unlisted_recipient, reject_unknown_sender_domain, reject_non_fqdn_sender, reject_rbl_client zen.spamhaus.org, reject_rbl_client bl.spamcop.net, reject_rbl_client b.barracudacentral.org, reject_rhsbl_reverse_client dbl.spamhaus.org, reject_rhsbl_sender dbl.spamhaus.org, reject_unauth_destination"
    - "smtpd_client_restrictions = permit_mynetworks, reject_unknown_client_hostname"
    - "smtpd_helo_required = yes"
    - "smtpd_helo_restrictions = reject_invalid_helo_hostname"
    - "smtpd_sender_restrictions = reject_non_fqdn_sender, reject_unknown_sender_domain"
    - "smtpd_client_message_rate_limit = 100"
    - "smtpd_client_connection_rate_limit = 30"
    - "smtpd_client_connection_count_limit = 10"
    - "header_checks = regexp:/etc/postfix/header_checks"
  register: postfix_core_settings
  notify: restart postfix

- name: Assert Postfix core settings applied
  assert:
    that:
      - postfix_core_settings is succeeded

- name: Sync Postfix chroot hosts file
  copy:
    src: /etc/hosts
    dest: /var/spool/postfix/etc/hosts
    remote_src: true
  register: postfix_hosts_sync
  notify: reload postfix

- name: Assert Postfix chroot hosts file synced
  assert:
    that:
      - postfix_hosts_sync is succeeded

- name: Deploy header_checks
  copy:
    dest: /etc/postfix/header_checks
    content: |
      /Subject:.*FedEx.*delay/ REJECT phishing attempt
  register: deploy_header_checks
  notify: reload postfix

- name: Assert header_checks deployed
  assert:
    that:
      - deploy_header_checks is succeeded

- name: Create Fail2Ban postfix-auth filter
  copy:
    dest: /etc/fail2ban/filter.d/postfix-auth.local
    content: |
      [Definition]
      failregex = .*postfix/smtpd\[\d+\]: warning: .*?\[<HOST>\]: SASL .* authentication failed
      ignoreregex =
  register: fail2ban_filter
  notify: reload fail2ban

- name: Assert Fail2Ban filter created
  assert:
    that:
      - fail2ban_filter is succeeded

- name: Configure Fail2Ban postfix-auth jail
  copy:
    dest: /etc/fail2ban/jail.d/postfix.conf
    content: |
      [postfix-auth]
      enabled = true
      port = smtp,ssmtp,submission
      filter = postfix-auth
      logpath = /var/log/maillog
      maxretry = 2
      findtime = 600
      bantime = 604800
      backend = auto
  register: fail2ban_jail
  notify: reload fail2ban

- name: Assert Fail2Ban jail configured
  assert:
    that:
      - fail2ban_jail is succeeded

- name: Install postfix hardening script
  copy:
    src: postfix/fix-postfix.sh
    dest: /usr/local/bin/fix-postfix.sh
    owner: root
    group: root
    mode: '0755'
  register: postfix_script_install
  notify: run postfix fix script

- name: Assert postfix hardening script installed
  assert:
    that:
      - postfix_script_install is succeeded

- name: Install header_checks
  copy:
    src: postfix/header_checks
    dest: /etc/postfix/header_checks
    owner: root
    group: root
    mode: '0644'
  register: header_checks_install
  notify: run postfix fix script

- name: Assert header_checks installed
  assert:
    that:
      - header_checks_install is succeeded

- name: Create Plesk event handler
  command: >-
    plesk bin event_handler --create
    -event mail_management_status_update
    -priority 100
    -user root
    -command /usr/local/bin/fix-postfix.sh
  register: plesk_handler
  failed_when: false
  changed_when: "'already exists' not in (plesk_handler.stderr | default(''))"

- name: Assert Plesk event handler command executed
  assert:
    that:
      - plesk_handler.rc == 0 or ('already exists' in (plesk_handler.stderr | default('')))
